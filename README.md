# AI--for--misinformation
Build a MERN fake news detector using  Groq API. Users submit text/image → backend (Node/Express) calls Gemini → returns JSON (Fake/Real/Uncertain, confidence, reason). React UI + Three.js 3D background + GSAP animations for smooth UX.
